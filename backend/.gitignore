# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# env vars
.env

# rotating app logs (utils/log_manager.py) - dated rotations don't end in
# .log (e.g. backend.log.2026-07-29), so match the base name with a
# wildcard suffix too.
*.log
backend.log.*

flytio_keypair.pem
